export const metadata = {
  title: "Services",
  description: "Detail Services",
};

const ServicesLayout = ({ children }) => {
  return <section>{children}</section>;
};
export default ServicesLayout;
